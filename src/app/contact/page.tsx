import { Contact } from '@/components/Contact';
import React from 'react';

const contact = () => {
	return (
		<main className="mt-16 flex min-h-screen flex-col items-center text-xl">
			<Contact />
		</main>
	);
};

export default contact;
